.blog-item
  .item-image
    img(src="{{ post.image }}")
  .item-title
    h2 {{ post.title }}
    p(class="meta") {{ post.date | date_to_string }}
  .post-description
    p {{ post.excerpt }}
    //- .botaomais
    a(href="{{ post.url }}") leia +
